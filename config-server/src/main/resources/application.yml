spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///D:\projects\spring\microservices-parent\cloud-config-server
          clone-on-start: true
          default-label: master
  security:
    user:
      name: config_root
      password: P@ssw0rd
server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://eureka:password@localhost:8761/eureka/
  instance:
    prefer-ip-address: true