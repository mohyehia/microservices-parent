spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mohyehia/cloud-config-server.git
          clone-on-start: true
          username: <git_username>
          password: <git_password>
          default-label: master
  security:
    user:
      name: config_root
      password: P@ssw0rd
server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://eureka:password@localhost:8761/eureka/
  instance:
    prefer-ip-address: true