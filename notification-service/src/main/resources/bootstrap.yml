spring:
  application:
    name: notification-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      username: ${CONFIG_SERVER_USERNAME:config_root}
      password: ${CONFIG_SERVER_PASSWORD:P@ssw0rd}