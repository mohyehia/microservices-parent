spring:
  application:
    name: inventory-service
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  cloud:
    config:
      uri: http://config-server:8888
  config:
    activate:
      on-profile: docker